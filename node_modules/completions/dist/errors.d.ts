import { CustomError } from "ts-custom-error";
import { type Choice } from "./createCompletions";
export declare class CancelledCompletionError extends CustomError {
    choices: Choice[];
    constructor(choices: Choice[]);
}
export declare class UnrecoverableRemoteError extends CustomError {
    error: string;
    constructor(error: string);
}
